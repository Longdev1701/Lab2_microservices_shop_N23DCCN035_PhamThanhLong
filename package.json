{
  "name": "microservices-shop-railway",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "railway:build": "cd product-service && npm ci && npx prisma generate && cd ../auth-service && npm ci && npx prisma generate && cd ../order-service && npm ci && cd ../api-gateway && npm ci",
    "railway:start": "node scripts/start-all-services.js"
  }
}
